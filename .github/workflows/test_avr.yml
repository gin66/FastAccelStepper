name: Run tests with simavr

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-20.04

    steps:
    - uses: actions/checkout@v2
    - name: get platformio
      run: curl -fsSL https://raw.githubusercontent.com/platformio/platformio-core-installer/master/get-platformio.py -o get-platformio.py
    - name: install getplatformio
      run: python3 get-platformio.py
    - name: install platform atmelavr
      run: pio platform install "atmelavr"
    - name: install platform espressif32
	  run: pio platform install "espressif32"
    - name: make
      run: make -C sim_tests
